import { addons, types } from '@storybook/addons';

import { useParameter } from '@storybook/api';


addons.register('vue-codesource-gen',(api) =>{

    console.log(' vue3-codesource-addon is registered ')

})